<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Feed | GoNature</title>
  <link rel="stylesheet" href="/css/feed.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">

</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm border-bottom px-4">
    <div class="container-fluid d-flex justify-content-between align-items-center">
      <a class="navbar-brand fw-bold text-success fs-4" href="#">GoNature 🌿</a>

      <div class="d-flex align-items-center position-relative">
        <!-- Dynamic Search bar (autocomplete) -->
        <div class="me-3 position-relative" style="min-width: 250px;">
          <input
            class="form-control"
            type="search"
            placeholder="Search users or groups"
            id="searchQuery"
            aria-label="Search"
          />
          <ul
            id="navSearchResults"
            class="list-group position-absolute top-100 start-0 w-100 shadow-sm z-3"
            style="max-height: 200px; overflow-y: auto;"
          ></ul>
        </div>

        <!-- Profile + Logout -->
        <a id="profileLink" href="#">
          <img id="userProfileImage" src="/images/profile-default.png"
            class="rounded-circle me-2 border" width="40" height="40" alt="Profile" />
        </a>
        <span id="usernameDisplay" class="fw-semibold me-3"></span>
        <a href="/api/auth/logout" class="btn btn-outline-success btn-sm">Logout</a>
      </div>
    </div>
  </nav>

  <!-- Feed content -->
  <div class="container mt-4" id="feed-container">

    <!-- Filter section -->
    <div class="container my-3" id="filter-section">
      <div class="card p-3 shadow-sm">
        <div class="row g-2 align-items-center">

          <div class="col-md-3">
            <input type="text" class="form-control" id="filterUsername" placeholder="Search by username">
          </div>

          <div class="col-md-3">
            <select class="form-select" id="filterGroup">
              <option value="">All Groups</option>
            </select>
          </div>

          <div class="col-md-3">
            <div class="position-relative">
              <input type="text" class="form-control pe-5" id="filterDateRange" placeholder="Date range" />
              <button type="button" id="clearDateFilterBtn"
                class="btn btn-sm btn-link position-absolute top-50 end-0 translate-middle-y me-2 text-danger"
                style="text-decoration: none;" aria-label="Clear date">
                &times;
              </button>
            </div>
          </div>

        </div>
      </div>
    </div>

    <div class="container my-3">
      <!-- Toggle button -->
      <div class="text-center mb-3">
        <button class="btn btn-outline-primary" type="button" data-bs-toggle="collapse"
          data-bs-target="#createPostCollapse" aria-expanded="false" aria-controls="createPostCollapse">
          + Create New Post
        </button>
      </div>

      <!-- Collapsible post form -->
      <div class="collapse" id="createPostCollapse">
        <form id="newPostForm" class="card shadow-sm p-3" enctype="multipart/form-data">
          <h5>Create New Post</h5>

          <!-- Alert placeholder -->
          <div id="alertContainer" class="mt-3"></div>

          <div class="mb-3">
            <label for="postGroup" class="form-label">Choose a Group:</label>
            <select class="form-select" id="postGroup">
              <option selected disabled>-- Select a group --</option>
            </select>
          </div>

          <div class="mb-3">
            <textarea id="newPostContent" class="form-control" placeholder="What's on your mind?" rows="2"></textarea>
          </div>

          <div class="row mb-3">
            <div class="col-md-6">
              <label class="form-label">Upload Image 📷</label>
              <div class="d-flex align-items-center">
                <button id="mediaUploadBtn" type="button" class="btn btn-outline-secondary btn-sm me-2">Choose
                  Image</button>
                <span id="mediaFileName">No file selected</span>
              </div>
              <input type="file" id="newPostMedia" accept="image/*" class="d-none">
                <div id="mediaPreview" style="display: none;" class="mt-2 position-relative">
                  <button type="button" id="removeMediaBtn"
                    class="btn-close position-absolute top-0 end-0" aria-label="Remove media"
                    style="z-index: 10;"></button>
                  <img id="previewImage" class="img-fluid rounded mb-2" style="max-height: 200px; display: none;" />
                  <video id="previewVideo" class="img-fluid rounded" controls style="max-height: 200px; display: none;"></video>
                </div>
            </div>

            <div class="col-md-6">
              <label class="form-label">Add Location:</label>
              <input id="locationSearch" class="form-control mb-2" type="text" placeholder="Search for a place..." />
              <div id="map" style="height: 200px; border-radius: 0.5rem;"></div>
              <input type="hidden" id="locationLat" />
              <input type="hidden" id="locationLng" />
            </div>
          </div>

          <button id="postSubmitBtn" class="btn btn-success" type="submit">Post</button>
        </form>
      </div>
    </div>


    <!-- Posts Area -->
    <div id="posts-container" class="row">
      <!-- Posts will be injected here -->
    </div>
  </div>

  <!-- Scripts -->
  <script src="/js/feed.js"></script>
  <script src="/js/mapSetup.js"></script>
  <script src="/js/navbar.js" defer></script>

  <!-- Google Maps API -->
  <script src="https://maps.googleapis.com/maps/api/js?key=<%= googleMapsApiKey %>&libraries=places&callback=initMap"
    async defer>
    </script>

  <!-- Bootstrap JS bundle (includes Popper for tooltips, dropdowns, collapse, etc.) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>

  <!-- Calendar -->
  <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>

</body>

</html>