<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Feed | GoNature</title>
  <link rel="stylesheet" href="/css/feed.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm border-bottom px-4">
    <div class="container-fluid d-flex justify-content-between align-items-center">
      <a class="navbar-brand fw-bold text-success fs-4" href="#">GoNature 🌿</a>
      <div class="d-flex align-items-center">
        <img id="userProfileImage" src="/images/profile-default.png" class="rounded-circle me-2 border" width="40"
          height="40" alt="Profile" />
        <span id="usernameDisplay" class="fw-semibold me-3"></span>
        <a href="/api/auth/logout" class="btn btn-outline-success btn-sm">Logout</a>
      </div>
    </div>
  </nav>

  <!-- Feed content -->
  <div class="container mt-4" id="feed-container">

    <!-- Search Bar -->
    <div class="input-group mb-4">
      <span class="input-group-text bg-white border-end-0"><i class="bi bi-search"></i></span>
      <input type="text" id="postSearchInput" class="form-control border-start-0" placeholder="Search posts..." />
    </div>

    <div class="container my-3">
      <!-- Toggle button -->
      <div class="text-center mb-3">
        <button class="btn btn-outline-primary" type="button" data-bs-toggle="collapse"
          data-bs-target="#createPostCollapse" aria-expanded="false" aria-controls="createPostCollapse">
          + Create New Post
        </button>
      </div>

      <!-- Collapsible post form -->
      <div class="collapse" id="createPostCollapse">
        <div class="card shadow-sm p-3" id="create-post-card">
          <h5>Create New Post</h5>

          <div class="mb-3">
            <label for="postGroup" class="form-label">Choose a Group:</label>
            <select class="form-select" id="postGroup">
              <option selected disabled>-- Select a group --</option>
            </select>
          </div>

          <div class="mb-3">
            <textarea id="newPostContent" class="form-control" placeholder="What's on your mind?" rows="2"></textarea>
          </div>

          <div class="row mb-3">
            <div class="col-md-6">
              <label class="form-label">Upload Image 📷</label>
              <div class="d-flex align-items-center">
                <button id="mediaUploadBtn" type="button" class="btn btn-outline-secondary btn-sm me-2">Choose
                  Image</button>
                <span id="mediaFileName">No file selected</span>
              </div>
              <input type="file" id="newPostMedia" accept="image/*" class="d-none">
              <div id="mediaPreview" style="display: none;" class="mt-2">
                <img id="previewImage" class="img-fluid rounded" />
              </div>
            </div>

            <div class="col-md-6">
              <label class="form-label">Add Location:</label>
              <input id="locationSearch" class="form-control mb-2" type="text" placeholder="Search for a place..." />
              <div id="map" style="height: 200px; border-radius: 0.5rem;"></div>
              <input type="hidden" id="locationLat" />
              <input type="hidden" id="locationLng" />
            </div>
          </div>

          <button id="postSubmitBtn" class="btn btn-success">Post</button>
        </div>
      </div>
    </div>


    <!-- Posts Area -->
    <div id="posts-container" class="row">
      <!-- Posts will be injected here -->
    </div>
  </div>

  <!-- Scripts -->
  <script src="/js/feed.js"></script>
  <script src="/js/mapSetup.js"></script>

  <!-- Google Maps API -->
  <script src="https://maps.googleapis.com/maps/api/js?key=<%= googleMapsApiKey %>&libraries=places&callback=initMap"
    async defer>
    </script>

  <!-- Bootstrap JS bundle (includes Popper for tooltips, dropdowns, collapse, etc.) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" defer></script>


</body>

</html>