<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register | GoNature</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Bootstrap Icons (for info icon tooltip) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/register.css" />
</head>

<body>
  <div class="container">
    <div class="register-container shadow">
      <h2 class="text-center">Join <span class="gonature">GoNature</span> 🌿</h2>

      <!-- Error message container -->
      <div id="errorContainer" class="alert alert-danger d-none text-center"></div>

      <form id="registerForm" action="/api/auth/register" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="username" class="form-label">Username</label>
          <input type="text" name="username" id="username" class="form-control" required
            placeholder="Choose a username" />
        </div>

        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input type="email" name="email" id="email" class="form-control" required placeholder="you@example.com" />
        </div>

        <div class="mb-3">
          <label for="password" class="form-label d-flex justify-content-between align-items-center">
            Password
            <span class="text-muted" data-bs-toggle="tooltip" data-bs-placement="top"
              title="At least 6 characters. Use a mix of uppercase, lowercase, numbers, and symbols.">
              <i class="bi bi-info-circle"></i>
            </span>
          </label>
          <input type="password" name="password" id="password" class="form-control" required
            placeholder="Create a password" />
          <div id="passwordStrength" class="mt-1 small"></div>
        </div>

        <div class="mb-3">
          <label for="confirmPassword" class="form-label">Confirm Password</label>
          <input type="password" name="confirmPassword" id="confirmPassword" class="form-control" required
            placeholder="Repeat your password" />
        </div>

        <div class="mb-3">
          <label for="profileImage" class="form-label">Upload Profile Image (optional)</label>
          <input type="file" name="profileImage" id="profileImage" class="form-control" accept="image/*" />
        </div>

        <button type="submit" class="btn btn-nature w-100">Register</button>

        <p class="text-small text-center mt-3">
          Already have an account?
          <a href="/login">Login here</a>
        </p>
      </form>
    </div>
  </div>

  <!-- Bootstrap JS (required for tooltips) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

  <!-- Client-side JS -->
  <script src="/js/register.js"></script>
</body>

</html>